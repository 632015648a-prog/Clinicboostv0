{
  "_readme": "Copia este archivo a appsettings.Development.Local.json y rellena los valores reales. NUNCA subas el .json sin .example a git (ya está en .gitignore).",

  "Supabase": {
    "_comment": "OBLIGATORIO en local: JwtSecret debe ser EXACTAMENTE el 'JWT Secret' que muestra `supabase status` (no el valor genérico de appsettings.Development.json). Si no coinciden, login OK pero la API devuelve 401 en /api/*.",
    "JwtSecret": "PEGAR_JWT_SECRET_DESDE_supabase_status"
  },

  "Twilio": {
    "AccountSid": "ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "AuthToken": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
    "WebhookBaseUrl": "https://tu-subdominio.trycloudflare.com"
  },

  "OpenAI": {
    "ApiKey": "sk-proj-..."
  },

  "Anthropic": {
    "ApiKey": "sk-ant-..."
  }
}
